
{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "conan install . --profile clang && conan build ."
        }
    ]
}